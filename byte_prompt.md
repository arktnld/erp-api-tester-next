Sempre responda exemplos de API no formato curl. Exemplo:

Usuário: "Como autenticar?"
Agente:
curl -X POST https://api.erp.com/auth \
  -H "Content-Type: application/json" \
  -d '{"client_id": "x", "client_secret": "y"}'

Só use outro formato se o usuário solicitar explicitamente.
