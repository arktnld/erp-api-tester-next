# feat/infra Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Adicionar proteção SSRF, migrar campos JSON para JSONB, logging estruturado e formatter XML.

**Architecture:** Mudanças cirúrgicas em execute route, schema Prisma, server actions e componentes de resposta.

**Tech Stack:** Prisma/PostgreSQL JSONB, pino, Next.js API routes, Vitest.

---

### Task 1: Worktree

**Step 1: Criar worktree**

```bash
cd /home/arktnld/projects/erp-api-tester-next
git worktree add .worktrees/infra -b feat/infra
```

**Step 2: Copiar .env**

```bash
cp .env .worktrees/infra/.env
```

**Step 3: Verificar**

```bash
git worktree list
```

---

### Task 2: SSRF protection

**Files:**
- Create: `apps/web/lib/security.ts`
- Create: `apps/web/lib/security.test.ts`
- Modify: `apps/web/app/api/execute/route.ts`

**Step 1: Escrever teste falhando**

```ts
// apps/web/lib/security.test.ts
import { describe, it, expect } from 'vitest'
import { validatePublicUrl } from './security'

describe('validatePublicUrl', () => {
  it('permite URLs públicas', () => {
    expect(() => validatePublicUrl('https://api.totvs.com.br/v1')).not.toThrow()
    expect(() => validatePublicUrl('http://erp.empresa.com/api')).not.toThrow()
  })
  it('bloqueia localhost', () => {
    expect(() => validatePublicUrl('http://localhost:3000')).toThrow('bloqueada')
    expect(() => validatePublicUrl('http://127.0.0.1:8080')).toThrow('bloqueada')
  })
  it('bloqueia ranges privados', () => {
    expect(() => validatePublicUrl('http://10.0.0.1')).toThrow('bloqueada')
    expect(() => validatePublicUrl('http://192.168.1.1')).toThrow('bloqueada')
    expect(() => validatePublicUrl('http://172.16.0.1')).toThrow('bloqueada')
    expect(() => validatePublicUrl('http://169.254.169.254')).toThrow('bloqueada')
  })
  it('bloqueia esquemas não-HTTP', () => {
    expect(() => validatePublicUrl('file:///etc/passwd')).toThrow()
    expect(() => validatePublicUrl('ftp://server.com')).toThrow()
  })
  it('rejeita URL inválida', () => {
    expect(() => validatePublicUrl('not-a-url')).toThrow()
  })
})
```

**Step 2: Rodar para confirmar falha**

```bash
cd .worktrees/infra/apps/web && npx vitest run lib/security.test.ts
```

Expected: FAIL "Cannot find module './security'"

**Step 3: Implementar**

```ts
// apps/web/lib/security.ts
const PRIVATE_PATTERNS = [
  /^127\./,
  /^10\./,
  /^192\.168\./,
  /^172\.(1[6-9]|2\d|3[01])\./,
  /^169\.254\./,
  /^::1$/,
  /^fc00:/i,
  /^fe80:/i,
]

export function validatePublicUrl(url: string): void {
  let parsed: URL
  try {
    parsed = new URL(url)
  } catch {
    throw new Error(`URL inválida: ${url}`)
  }
  if (parsed.protocol !== 'http:' && parsed.protocol !== 'https:') {
    throw new Error(`Protocolo não permitido: ${parsed.protocol}`)
  }
  const host = parsed.hostname.toLowerCase()
  if (host === 'localhost' || host === '0.0.0.0') {
    throw new Error(`URL bloqueada (SSRF): ${host}`)
  }
  for (const pattern of PRIVATE_PATTERNS) {
    if (pattern.test(host)) {
      throw new Error(`URL bloqueada (SSRF): ${host}`)
    }
  }
}
```

**Step 4: Rodar para confirmar passou**

```bash
npx vitest run lib/security.test.ts
```

Expected: PASS (6 testes)

**Step 5: Integrar no execute route**

Em `apps/web/app/api/execute/route.ts`:
- Adicionar import: `import { validatePublicUrl } from '@/lib/security'`
- Adicionar chamada logo antes do `httpFetch`:

```ts
const url = `${environmentUrl ?? company.baseUrl}${resolvedPath}`

try {
  validatePublicUrl(url)
} catch (err) {
  return NextResponse.json({ error: String(err) }, { status: 400 })
}

const requestHeaders: Record<string, string> = { ... }
```

**Step 6: Rodar todos os testes**

```bash
npx vitest run
```

Expected: todos passando

**Step 7: TypeScript check**

```bash
npx tsc --noEmit --skipLibCheck 2>&1 | grep -v tsbuildinfo
```

Expected: sem erros

**Step 8: Commit**

```bash
cd /home/arktnld/projects/erp-api-tester-next/.worktrees/infra
git add apps/web/lib/security.ts apps/web/lib/security.test.ts apps/web/app/api/execute/route.ts
git commit -m "feat(infra): add SSRF protection to execute endpoint"
```

---

### Task 3: JSONB migration

Migrar `Company.authConfig`, `Company.environments`, `TestClient.fieldsData` de `String` para `Json` (JSONB no PostgreSQL).

**Files:**
- Modify: `packages/db/prisma/schema.prisma`
- Modify: `apps/web/lib/actions/companies.ts`
- Modify: `apps/web/lib/actions/test-clients.ts`
- Modify: `apps/web/app/api/execute/route.ts`
- Modify: `apps/web/app/test/test-page.tsx`
- Modify: `apps/web/app/test/lib/types.ts`
- Modify: `apps/web/app/companies/companies-client.tsx` (linha 82)
- Modify: `apps/web/app/companies/[id]/company-detail-client.tsx` (linha 88)
- Modify: `apps/web/app/companies/[id]/clients/client-builder.tsx` (linha 70)
- Modify: `apps/web/app/test/lib/utils.ts` (linha 16)

**Step 1: Atualizar schema.prisma**

Localizar campos em `Company` e `TestClient` e alterar:

```prisma
model Company {
  // antes: environments String @default("[]")
  // antes: authConfig   String @default("{}")
  environments Json @default("[]")
  authConfig   Json @default("{}")
  ...
}

model TestClient {
  // antes: fieldsData String @default("{}")
  fieldsData Json @default("{}")
  ...
}
```

**Step 2: Criar e aplicar migration**

```bash
cd /home/arktnld/projects/erp-api-tester-next/.worktrees/infra/packages/db
npx prisma migrate dev --name jsonb-json-fields
```

Expected: Migration criada e aplicada. SQL gerado usa `ALTER COLUMN ... TYPE jsonb USING ...::jsonb`.

**Step 3: Regenerar Prisma client**

```bash
npx prisma generate
```

**Step 4: Atualizar companies.ts**

Na ação `createCompany` e `updateCompany`, parsear strings antes de salvar:

```ts
// createCompany:
await prisma.company.create({
  data: {
    ...parsed,
    environments: JSON.parse(parsed.environments),
    authConfig: JSON.parse(parsed.authConfig),
  }
})

// updateCompany:
await prisma.company.update({
  where: { id },
  data: {
    ...parsed,
    environments: JSON.parse(parsed.environments),
    authConfig: JSON.parse(parsed.authConfig),
  }
})
```

**Step 5: Atualizar test-clients.ts**

```ts
// createTestClient:
await prisma.testClient.create({
  data: { ...parsed, fieldsData: JSON.parse(parsed.fieldsData) }
})

// updateTestClient:
await prisma.testClient.update({
  where: { id },
  data: { ...parsed, fieldsData: JSON.parse(parsed.fieldsData) }
})
```

**Step 6: Atualizar types.ts**

```ts
export type TestClient = {
  id: number
  name: string
  fieldsData: Record<string, string>
}

export type Company = {
  ...
  environments: Environment[]
  authConfig: Record<string, string> | null
  ...
}
```

**Step 7: Atualizar todos os JSON.parse nos arquivos de leitura**

Nos 6 arquivos listados, substituir `JSON.parse(campo)` por cast direto:

- `execute/route.ts`:
  - `JSON.parse(client.fieldsData)` → `client.fieldsData as Record<string, string>`
  - `JSON.parse(company.authConfig || '{}')` → `(company.authConfig ?? {}) as Record<string, string>`

- `test-page.tsx`:
  - `JSON.parse(company.environments || '[]')` → `(company.environments ?? []) as Environment[]`
  - `JSON.parse(client.fieldsData)` → `client.fieldsData as Record<string, string>`
  - `JSON.parse(company.authConfig || '{}')` → `(company.authConfig ?? {}) as Record<string, string>`

- `test/lib/utils.ts`:
  - `JSON.parse(company.authConfig || '{}')` → `(company.authConfig ?? {}) as Record<string, string>`

- `companies-client.tsx`:
  - `JSON.parse(company.environments)` → `(company.environments ?? []) as Environment[]`

- `company-detail-client.tsx`:
  - `JSON.parse(client.fieldsData)` → `client.fieldsData as Record<string, string>`

- `client-builder.tsx`:
  - `JSON.parse(client.fieldsData)` → `client.fieldsData as Record<string, string>`

**Step 8: TypeScript check**

```bash
cd /home/arktnld/projects/erp-api-tester-next/.worktrees/infra/apps/web
npx tsc --noEmit --skipLibCheck 2>&1 | grep -v tsbuildinfo
```

Resolver todos os erros de tipo.

**Step 9: Rodar testes**

```bash
npx vitest run
```

Expected: todos passando

**Step 10: Commit**

```bash
cd /home/arktnld/projects/erp-api-tester-next/.worktrees/infra
git add -A
git commit -m "feat(infra): migrate authConfig, environments, fieldsData to JSONB"
```

---

### Task 4: Pino structured logging

**Files:**
- Install: pino
- Create: `apps/web/lib/logger.ts`
- Modify: `apps/web/app/api/execute/route.ts`

**Step 1: Instalar pino**

```bash
cd /home/arktnld/projects/erp-api-tester-next/.worktrees/infra/apps/web
pnpm add pino
pnpm add -D @types/pino
```

**Step 2: Criar logger**

```ts
// apps/web/lib/logger.ts
import pino from 'pino'

export const logger = pino({ level: process.env.LOG_LEVEL ?? 'info' })
```

**Step 3: Adicionar logging no execute route**

No POST handler de `apps/web/app/api/execute/route.ts`:

```ts
import { logger } from '@/lib/logger'

// Logo após construir url:
logger.info({ endpointId, url, method: endpoint.method }, 'execute start')

// Após httpFetch (linha ~201), antes de criar RequestHistory:
logger.info({ url, statusCode, durationMs }, 'execute complete')

// No catch do httpFetch (linha ~197):
logger.warn({ url, err: String(err) }, 'execute http error')

// No catch externo (linha ~237):
logger.error({ err: String(err) }, 'execute server error')
```

**Step 4: TypeScript check**

```bash
npx tsc --noEmit --skipLibCheck 2>&1 | grep -v tsbuildinfo
```

**Step 5: Commit**

```bash
cd /home/arktnld/projects/erp-api-tester-next/.worktrees/infra
git add apps/web/lib/logger.ts apps/web/app/api/execute/route.ts apps/web/package.json pnpm-lock.yaml
git commit -m "feat(infra): add pino structured logging to execute endpoint"
```

---

### Task 5: XML response formatter

**Files:**
- Modify: `apps/web/lib/utils.ts`
- Modify: `apps/web/app/test/components/test-response.tsx` (apenas se usar XML)

**Step 1: Adicionar tryPrettyXml em utils.ts**

```ts
export function tryPrettyXml(str: string): string {
  try {
    const INDENT = '  '
    let indent = 0
    const tokens = str.match(/<[^>]+>|[^<]+/g) ?? []
    const lines: string[] = []
    for (const token of tokens) {
      const trimmed = token.trim()
      if (!trimmed) continue
      if (trimmed.startsWith('</')) {
        indent = Math.max(0, indent - 1)
        lines.push(INDENT.repeat(indent) + trimmed)
      } else if (
        trimmed.startsWith('<') &&
        !trimmed.startsWith('<?') &&
        !trimmed.startsWith('<!') &&
        !trimmed.endsWith('/>')
      ) {
        lines.push(INDENT.repeat(indent) + trimmed)
        indent++
      } else {
        lines.push(INDENT.repeat(indent) + trimmed)
      }
    }
    const result = lines.join('\n').trim()
    return result || str
  } catch {
    return str
  }
}
```

**Step 2: Usar no test-response.tsx**

Ler `apps/web/app/test/components/test-response.tsx`. Localizar onde o body é exibido com `language="xml"` e aplicar `tryPrettyXml(responseBody)` antes de passar ao CodeBlock.

**Step 3: Rodar testes**

```bash
npx vitest run
```

**Step 4: Commit**

```bash
cd /home/arktnld/projects/erp-api-tester-next/.worktrees/infra
git add apps/web/lib/utils.ts apps/web/app/test/components/test-response.tsx
git commit -m "feat(infra): add XML response formatter"
```

---

### Task 6: Finish branch

**Step 1: Rodar todos os testes**

```bash
cd /home/arktnld/projects/erp-api-tester-next/.worktrees/infra/apps/web
npx vitest run
```

Expected: todos passando

**Step 2: TypeScript check final**

```bash
npx tsc --noEmit --skipLibCheck 2>&1 | grep -v tsbuildinfo
```

Expected: sem erros

**Step 3: Usar finishing-a-development-branch skill**
