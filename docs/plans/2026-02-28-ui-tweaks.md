# UI Tweaks — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 5 pequenas melhorias de UX: numeração nos selects de teste, tooltip no botão Importar, renomear "Configurado", tooltip em URLs, e toda a linha do ERP clicável.

**Architecture:** Mudanças pontuais em 6 arquivos. Sem novos componentes. A linha do ERP clicável requer adicionar prop `onRowClick` ao DataTable e `e.stopPropagation()` nos botões de ação.

**Tech Stack:** React, Next.js, TanStack Table.

---

### Task 1: Set up worktree

**Step 1:**
```bash
cd /home/arktnld/projects/erp-api-tester-next
git worktree add .worktrees/ui-tweaks -b feat/ui-tweaks
cd .worktrees/ui-tweaks
pnpm install
```

**Step 2:**
```bash
git commit --allow-empty -m "chore: start ui-tweaks"
```

---

### Task 2: Numeração nos selects de teste

**Files:**
- Modify: `apps/web/app/test/components/test-selectors.tsx:95,101,128,139`

Trocar os 4 labels de `<p>` de:
```tsx
<p style={sectionLabel}>ERP</p>
...
<p style={sectionLabel}>Empresa</p>
...
<p style={sectionLabel}>Endpoint</p>
...
<p style={{ ...sectionLabel, opacity: !needsClient ? 0.4 : 1 }}>Cliente de Teste</p>
```

Para:
```tsx
<p style={sectionLabel}>1. ERP</p>
...
<p style={sectionLabel}>2. Empresa</p>
...
<p style={sectionLabel}>3. Endpoint</p>
...
<p style={{ ...sectionLabel, opacity: !needsClient ? 0.4 : 1 }}>4. Cliente de Teste</p>
```

**Verify:** http://localhost:3000/test — sidebar mostra "1. ERP", "2. Empresa", "3. Endpoint", "4. Cliente de Teste".

**Commit:**
```bash
git add apps/web/app/test/components/test-selectors.tsx
git commit -m "feat(ui): add step numbers to test selectors"
```

---

### Task 3: Tooltip no botão Importar e renomear "Configurado"

**Files:**
- Modify: `apps/web/app/home-import.tsx` — botão Importar (~linha 156)
- Modify: `apps/web/app/chat/chat-client.tsx:623`

**home-import.tsx** — adicionar `title` ao botão Importar:
```tsx
<button
  onClick={() => setOpen(true)}
  title="Importar endpoint via curl"
  style={{ ... }}
>
  Importar
</button>
```

**chat-client.tsx** — linha 623, de:
```tsx
<span>{keysConfigured ? 'Configurado' : 'Configurações'}</span>
```
Para:
```tsx
<span>Configurar IA</span>
```

**Verify:**
- Na homepage, hover no botão "Importar" — tooltip aparece
- Na página /chat, botão no rodapé esquerdo mostra "Configurar IA"

**Commit:**
```bash
git add apps/web/app/home-import.tsx apps/web/app/chat/chat-client.tsx
git commit -m "feat(ui): add import tooltip and rename Configurado to Configurar IA"
```

---

### Task 4: Tooltip em URLs longas de empresas

**Files:**
- Modify: `apps/web/app/companies/companies-client.tsx:163-166`

De:
```tsx
cell: ({ row }) => (
  <span style={{ fontSize: 12, color: 'var(--text-muted)', fontFamily: 'monospace' }}>
    {row.original.baseUrl || <span style={{ color: 'var(--text-subtle)' }}>—</span>}
  </span>
),
```

Para:
```tsx
cell: ({ row }) => (
  <span
    title={row.original.baseUrl || undefined}
    style={{ fontSize: 12, color: 'var(--text-muted)', fontFamily: 'monospace' }}
  >
    {row.original.baseUrl || <span style={{ color: 'var(--text-subtle)' }}>—</span>}
  </span>
),
```

**Verify:** http://localhost:3000/companies — hover sobre uma URL longa, tooltip mostra a URL completa.

**Commit:**
```bash
git add apps/web/app/companies/companies-client.tsx
git commit -m "feat(ui): add title tooltip to company base URLs"
```

---

### Task 5: Linha inteira do ERP clicável

**Files:**
- Modify: `apps/web/components/ui/data-table.tsx` — adicionar prop `onRowClick`
- Modify: `apps/web/app/erps/erps-client.tsx` — usar `onRowClick` e adicionar `stopPropagation` nos botões

**data-table.tsx** — adicionar `onRowClick` opcional às props:

Localizar a interface de props (antes do `export function DataTable`) e adicionar:
```tsx
onRowClick?: (row: T) => void
```

No `<tr>` das linhas de dados, adicionar `onClick` e `cursor`:
```tsx
<tr
  key={row.id}
  style={{
    borderBottom: '1px solid var(--border)',
    cursor: onRowClick ? 'pointer' : undefined,
  }}
  onClick={() => onRowClick?.(row.original)}
  onMouseEnter={(e) => { e.currentTarget.style.backgroundColor = 'var(--surface-2)' }}
  onMouseLeave={(e) => { e.currentTarget.style.backgroundColor = 'transparent' }}
>
```

**erps-client.tsx** — usar `onRowClick` e adicionar `stopPropagation` nos botões Edit e Delete:

No topo do arquivo adicionar `import { useRouter } from 'next/navigation'` e dentro do componente `const router = useRouter()`.

Nos botões Edit e Delete, adicionar `e.stopPropagation()`:
```tsx
<Button
  variant="ghost"
  size="sm"
  onClick={(e) => { e.stopPropagation(); setSheet({ open: true, erp: row.original }) }}
>
  <Pencil size={13} />
</Button>
<Button
  variant="ghost"
  size="sm"
  onClick={(e) => { e.stopPropagation(); startTransition(() => deleteERP(row.original.id)) }}
>
  <Trash2 size={13} />
</Button>
```

No `<DataTable>`, adicionar:
```tsx
onRowClick={(row) => router.push(`/erps/${row.id}`)}
```

**Verify:** http://localhost:3000/erps — clicar em qualquer parte da linha navega para o ERP. Clicar no lápis ou lixeira não navega.

**Commit:**
```bash
git add apps/web/components/ui/data-table.tsx apps/web/app/erps/erps-client.tsx
git commit -m "feat(ui): make entire ERP table row clickable"
```

---

### Task 6: Finish branch

> REQUIRED SUB-SKILL: Use superpowers:finishing-a-development-branch
